## 1. 共通グラフ選択

- [x] 1.1 Matrix Market読込後の無向グラフについて全連結成分を`O(n+m)`で列挙する共通APIを追加する
- [x] 1.2 最大連結成分を決定的に選択し、内部頂点を再番号付けして元頂点ID・元／採用統計を保持するGraph APIを実装する
- [x] 1.3 最大成分が実行不能な場合の統計付きエラーを実装する
- [x] 1.4 非連結・孤立頂点・同率成分・連結入力・自己ループのみの単体テストを追加する

## 2. CPU/GPU通常実行への統合

- [x] 2.1 CPU Sparse SGDの通常入力経路を常時最大連結成分選択へ変更し、既存の位置引数とSGDパラメータを維持する
- [x] 2.2 GPU Sparse SGDの通常入力経路を同じ共通Graph APIへ変更し、CPU版と同じ縮約グラフを使用する
- [x] 2.3 両実行ファイルで入力統計と最大成分の頂点数・辺数・成分数・採用率を常にターミナル表示する
- [x] 2.4 結果TXTへ成分統計を、出力prefixへ0始まりvertex mapを常に保存する
- [x] 2.5 既存のCLI引数、連結入力、旧`--largest-component`指定が互換に実行できることをテストする

## 3. 実行・回帰検証

- [x] 3.1 同じ非連結fixtureでCPU版・GPU版の採用頂点列、辺、統計、mapが一致することを検証する
- [x] 3.2 `bcsstk31.mtx`を追加フラグなしでCPU版・GPU版へ実行し、最大成分統計と有限座標を確認する
- [x] 3.3 `bcsstm35.mtx`を追加フラグなしで実行し、小さい採用率がターミナルと結果へ明記されることを確認する
- [x] 3.4 `USpowerGrid.mtx`を追加フラグなしで実行し、全頂点採用と既存のstress品質を確認する
- [x] 3.5 抽出後のGPU結果を`gpu_visualizer`で読み込み、stress計算とPNG生成を確認する

## 4. 完了確認

- [x] 4.1 各crateで`cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`を成功させる
- [x] 4.2 実行コマンド、端末表示、抽出統計、代表出力をOpenSpec verification reportへ記録する
