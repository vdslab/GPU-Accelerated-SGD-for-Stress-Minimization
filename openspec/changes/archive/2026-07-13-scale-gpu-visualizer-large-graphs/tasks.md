## 1. CLIと計測基盤

- [x] 1.1 `gpu_visualizer` の引数解析を整理し、従来の対話的ファイル名入力を維持したまま `--stress auto|exact|sampled|off`、`--stress-samples`、`--stress-seed`、`--node-radius`、`--max-edges` を追加する
- [x] 1.2 読込、ストレス、GPU準備・転送/描画/読戻し、PNG encode、合計を個別に計測・表示する実行結果構造を追加する
- [x] 1.3 不正なモード、0標本、不正な半径、0の辺上限を具体的なメッセージで拒否するCLIテストを追加する

## 2. 有界なストレス評価

- [x] 2.1 従来の厳密ストレス計算を結果種別付きAPIへ分離し、既存fixtureの値を許容誤差内で固定する回帰テストを追加する
- [x] 2.2 seedから重複なしの始点を決定的に選び、`|V| / (2K) * Σ C_i` を計算する標本化ストレスを実装する
- [x] 2.3 同一seedの再現性、異なる標本数、`K >= |V|` で厳密値に一致すること、非連結頂点対を無視する既存挙動を単体テストする
- [x] 2.4 `auto` の8,000頂点閾値、既定64標本、`exact` の大規模警告、`off` のBFS省略をmain処理へ統合する
- [x] 2.5 厳密値を `Stress`、推定値を `Approx stress` として標本数・seed付きで表示し、ログ上で混同できないことを統合テストする

## 3. 入力の大規模対応

- [x] 3.1 結果TXTのnode/edge countを読込時のcapacity予約へ利用し、既存出力形式との互換性を維持する
- [x] 3.2 座標数、NaN/無限座標、`u32` 頂点数上限、範囲外の辺端点をGPU初期化前に検出する
- [x] 3.3 不正入力が該当頂点または辺を含む診断を返す単体テストを追加する

## 4. インデックスGPUレンダラー

- [x] 4.1 正規化済みノード座標を単一vertex bufferへ構築し、ノードのinstance入力と辺描画で共有できる形へ変更する
- [x] 4.2 辺端点を `u32` index bufferへ格納し、line-list pipelineを `draw_indexed` で全辺描画するよう変更して、展開済み辺座標ベクタを削除する
- [x] 4.3 adapter/device limitと必要なvertex/index/readback bufferサイズを比較し、確保前に必要量と上限を含むエラーを返す
- [x] 4.4 小規模fixtureで描画頂点数・辺数・PNG寸法を検証し、辺バッファ量が `2 * |E| * sizeof(u32)` であることをテストする

## 5. LODと明示的な辺標本化

- [x] 5.1 出力解像度と頂点数からノード半径を選ぶ自動LODを実装し、25万頂点・2048×2048で4px未満になるテストを追加する
- [x] 5.2 `--node-radius` の上書きをレンダラーへ反映し、実際に使用した半径をターミナルへ表示する
- [x] 5.3 `--max-edges` 指定時だけseedに基づく決定的な辺標本化を行い、描画辺数・総辺数・間引きの有無を表示する
- [x] 5.4 辺数上限を指定しない通常実行では全辺がindex bufferへ格納される回帰テストを追加する

## 6. 大規模受入確認

- [x] 6.1 巨大ファイルをコミットせずに25万頂点・190万辺以上200万辺以下の連結グラフと有限座標を決定的に生成するfixture/補助コマンドを追加する
- [x] 6.2 CPUテストで大規模fixtureが `auto` 時に厳密経路へ入らないこと、index buffer構築量、入力検証、決定的標本化を確認する
- [x] 6.3 対応する実GPUで2048×2048 PNGを生成し、GPU validation error・メモリ不足がないことと、全辺描画時の段階別時間・使用半径・ストレス条件を記録する
- [x] 6.4 既存の30,000頂点超の結果TXTでも通常の `cargo run` が近似ストレス経路を使用して完了することを確認し、READMEへ新オプションと厳密値/近似値の違いを追記する
- [x] 6.5 `cargo fmt`、`cargo test`、`cargo clippy -- -D warnings` を実行し、全チェックを通す
