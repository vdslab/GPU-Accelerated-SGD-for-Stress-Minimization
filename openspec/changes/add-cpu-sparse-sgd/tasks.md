## 1. crate の準備とグラフ入力

- [x] 1.1 CPU 向け依存だけを持ち、既存ベースラインと同様に `main`、`graph`、`algorithm` モジュールへ分割した `baseline-sparse-sgd-non-gpu` Rust crate を作成する
- [x] 1.2 Matrix Market 形式のグラフ読み込み、自己ループの除外、無向隣接リストの構築、グラフメタデータの取得を実装する
- [x] 1.3 重複辺と自己ループを含む、グラフ読み込みおよび隣接リストの単体テストを追加する

## 2. 疎なグラフ前処理

- [x] 2.1 到達不能を明示的に扱う、重みなし単一始点 BFS を実装する
- [x] 2.2 同率候補からのランダム選択、pivot の一意性、非連結成分の網羅、pivot 数の制限を備えたランダム化 max-min pivot 選択を実装する
- [x] 2.3 到達可能な頂点と pivot の組およびグラフの辺から、端点を正規化した重複のない制約を構築し、辺の距離を1、重みを `1 / d_ij^2` とする
- [x] 2.4 全点対距離行列を作らず、有限な重みの最小値と最大値を求めて指数減衰アニーリングスケジュールを生成する
- [x] 2.5 BFS、固定 seed による pivot 選択の不変条件、非連結グラフ、制約数と内容、学習率の端点に対する単体テストを追加する

## 3. CPU Sparse SGD

- [x] 3.1 ランダムな二次元レイアウトの初期化と、任意指定による座標の中心化を実装する
- [x] 3.2 制限付き `mu` と座標一致時の有限値処理を備えた、1制約分の対称 Sparse SGD 更新を実装する
- [x] 3.3 学習率ごとに疎制約をシャッフルし、必要に応じて中心化したレイアウトを返すアニーリング反復を実装する
- [x] 3.4 大きさが等しく向きが反対の更新、距離0の安全性、座標の有限性、スケジュール長、中心化後の許容誤差に対する単体テストを追加する

## 4. 実行ファイルと結果出力

- [x] 4.1 `main.rs` でグラフ読み込み、pivot 数・反復回数・epsilon・中心化の明示的な設定、前処理、時間計測、Sparse SGD 実行を接続する
- [x] 4.2 `baseline-sparse-sgd-non-gpu` のタイムスタンプ付きファイル名接頭辞と段階別の末尾番号を使用し、初期レイアウトと処理後レイアウトを既存ベースライン互換のテキスト形式で書き出す
- [x] 4.3 既存の辺セクションと座標セクションを維持しつつ、Sparse SGD の出力を解釈するために必要なデータセット、グラフ、実行条件、pivot のメタデータを追加する

## 5. 検証

- [x] 5.1 新しい crate で `cargo fmt`、`cargo clippy -- -D warnings`、`cargo test` を実行し、すべての指摘を解消する
- [x] 5.2 小規模データセットでスモークテストを実行し、2つの出力ファイルが生成され、出力された全座標が有限であることを確認する
- [x] 5.3 既存の `baseline-sgd-non-gpu` のソースコードと動作が変更されていないことを確認する
